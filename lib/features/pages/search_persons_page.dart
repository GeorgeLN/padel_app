
import 'package:flutter/material.dart';

class SearchPersonsPage extends StatelessWidget {
   
  const SearchPersonsPage({super.key});
  
  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
         child: Text('SearchPersonsPage'),
      ),
    );
  }
}